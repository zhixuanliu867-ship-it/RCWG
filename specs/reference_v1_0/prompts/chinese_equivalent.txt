中文鲁棒性轨模板（不是另一份主轨测试集）：
你需要生成可执行的计算工作流，而不是流程的说明。仅使用给定算子、数据和
WorkIR 1.0。保持任务语义、硬约束、证据要求和输出契约；明确算法实现、参数
和数据依赖。不要把节点更少等同于更高效，不得用近似结果代替精确要求。
只返回最终 JSON 对象；不要求披露内部推理。未知证据不等于否定事实。
任务：{{task_json}}
算子：{{operator_catalog}}
结构：{{workflow_schema}}
